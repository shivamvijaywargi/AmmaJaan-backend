// This will work with VS Code
// With this if we change any file then the docker compose will pick that and restart the server with those changes
// This is helpful during development
// SOURCE: https://www.youtube.com/watch?v=Xisj0FJSBIs
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "restart server",
      "type": "shell",
      "command": "docker compose restart server",
      "presentation": {
        "reveal": "never"
      }
    }
  ]
}
